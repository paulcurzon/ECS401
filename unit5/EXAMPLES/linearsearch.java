/* *****************************************AUTHOR Paul CurzonThis program demonstrates  - arrays. - linear search   Search for a given string******************************************** */import java.util.*; class linearsearch{    public static void main (String[] param)    {        String[] animals = {"cat","rat","wombat","cat","bat"};       String searchKey; //the thing looked for       int result = -1;        // the position where the answer is found             searchKey = input("Give me an animal to search for");       result = search(searchKey, animals);       System.out.println(searchKey + " was found in position " + result);       System.exit(0);    } // END main        /* ***************************************************       Search for a given key    */        public static int search(String key, String[] a)    {        for (int i=0; i<a.length; i++)        {           if (a[i].equals(key))           {              return i;              }       }       return -1; // NOT FOUND    } // END search    // Input strings    public static String input(String message)    {       Scanner scanner = new Scanner(System.in);       System.out.println(message);          return scanner.nextLine();    } // END input    } // END class linearsearch